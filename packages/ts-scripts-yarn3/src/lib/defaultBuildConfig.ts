export const defaultBuildConfig = {
  "compilerOptions": {
    rootDir: "src"
  },
  exclude: ['**/*.spec.*', '**/*.spec', '**/*.stories.*', '**/*.example.*', '**/spec/*', '**/stories/*'],
  include: ['src'],
}
